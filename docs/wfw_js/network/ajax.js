/**
 * Envoie une requête ajax sur l'url $url avec les paramètres $params
 * @function ajax
 * @memberOf wfw.network
 * @param {string} $url      Url pour la requête
 * @param {object} [$params] Paramètres
 */