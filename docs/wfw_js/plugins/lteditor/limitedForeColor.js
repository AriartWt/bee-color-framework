/**
 * Permet la création d'un panel de couleurs limitées à un subset particulier défini à l'avance
 * @function limitedForeColor
 * @memberOf wfw.ui.lteditor.plugins
 * @param {String[]} $colors Liste des couleurs (format css3) autorisées
 * @param {String} $default Couleur par défaut.
 */